/// Minimal stub implementation of the federated flutter_secure_storage web
/// plugin.
///
/// The application provides its own web-safe storage implementation via
/// [SecureKeyValueStore], so this class only needs to satisfy the plugin
/// registration entry point that Flutter tooling expects.
class FlutterSecureStorageWeb {
  /// Registers the stub with the plugin registrant.
  static void registerWith([Object? registrar]) {}
}
